class Image{
    constructor(id, categoryId, imageURL, imageColor)
    {
        this.id = id;
        this.categoryId = categoryId;
        this.imageURL = imageURL;
        this.imageColor = imageColor;
    };
}

export default Image;


